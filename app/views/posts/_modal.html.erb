<div  class="modal myModal"
      tabindex="-1"
      role="dialog"
      aria-labelledby="myModalLabel">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <span class="posted-by"></span>
        <button type="button"
                class="close"
                data-dismiss="modal"
                aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
        <div class="modal-body">
          <div class="loaded-data">
            <h3></h3>
            <p></p>
            <div class="interested"><a href="">I'm interested</a></div><br />
          </div><!-- loaded-data -->
        </div><!-- modal-body -->
    </div>
  </div>
</div>

<!--

<button id="editorBtn" type="button">Enable Editing</button>
<button id="saveEdit">Click to save</button>
<div class="delete-post" id="post_<@=@post.id%>"><###########%= link_to "delete post", @post, method: :delete, data: {confirm: "Are you sure?"}, remote: true %></div><br />
<div class="edit-post"><##########%= link_to "edit post", @post, method: :delete, data: {confirm: "Are you sure?"}, remote: true %></div><br />
<script type="text/javascript">
var editor = document.getElementById('editor');
editor.isContentEditable;
editor.contentEditable = true;
$('#saveEdit').click(function() {
  $edit = $('#editor').html();
  $.ajax({
    url: '/posts/118',
    type: 'post',
    data: {data: $edit},
    datatype: 'html',
    success: function(rsp){
      alert(rsp);
    }
  });
});
-->
